
# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[versioneer]
VCS = git
style = pep440
versionfile_source = ib_dl/_version.py
versionfile_build = ib_dl/_version.py
tag_prefix = ''
parentdir_prefix = ib_dl

